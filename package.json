{
  "name": "wire",
  "version": "1.25.0",
  "private": true,
  "description": "Only for the MCP function in api/. The pages have no dependencies and no build step.",
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "@supabase/supabase-js": "2.116.0",
    "zod": "4.6.4"
  }
}
