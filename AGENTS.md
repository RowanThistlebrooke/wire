# Working on The Wire

The laws are in [CLAUDE.md](CLAUDE.md). Read that file before you touch
anything. It is the only copy of them.

This file is a pointer on purpose. It used to be a second copy, and a law in
two places drifts: this one fell behind on the void, on the index gate, and
on where a secret is allowed to live. Two files then disagreed about the same
law, and whichever one an agent happened to open decided what it believed.

If you are adding a law or changing one, do it in CLAUDE.md. Do not write it
here as well.
